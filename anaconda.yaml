anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: mlflow/mlflow
      use_max_tag: true
      include_regex: ^v\d+\.\d+\.\d+$
      prefix: v
    packages:
      - mlflow
      - mlflow-skinny
